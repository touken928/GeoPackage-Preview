// ---------------------------------------------------------------------------
// Webview entry point — placeholder.
//
// The UI team will replace this file with the React application bootstrap
// (e.g. createRoot + render inside <div id="root">).
// ---------------------------------------------------------------------------

console.log('[GeoPackage Preview] Webview loaded. Ready to receive document.');
